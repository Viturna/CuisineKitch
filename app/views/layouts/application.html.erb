<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Balises pour une indexation optimisée -->
    <meta name="robots" content="index, follow">
    <meta name="googlebot" content="index, follow">
    <meta name="bingbot" content="index, follow">
    <!-- META -->
    <title><%= @page_title || "Cuisine Kitch" %></title>
    <meta name="description" content="<%= @meta_description || "Description par défaut" %>">
    <%= favicon_link_tag 'favicon.svg' %>
    <meta name="theme-color" content="#ffffff">
    <meta name="copyright" content="© Copyright CuisineKitch - Tous droits réservés.">
    <!-- CSS -->
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', 'custom', media: 'all' %>
    <%= javascript_importmap_tags %>
    <link rel="preload" as="font">
    <!-- Balises pour les réseaux sociaux -->
    <meta property="og:locale" content="fr_FR">
    <meta property="og:type" content="website">
    <meta property="og:title" content="<%= @page_title || "Cuisine Kitch" %>">
    <meta property="og:site_name" content="Cuisine Kitch">
    <meta property="og:url" content="<%= @current_page_url %>">
    <meta property="og:description" content="<%= @meta_description || "Description par défaut" %>">
    <meta property="og:image" content="/assets/images/favicon.svg">
    <!-- Balises pour Twitter -->
    <meta name="twitter:title" content="<%= @page_title || "Cuisine Kitch" %>">
    <meta name="twitter:description" content="<%= @meta_description || "Description par défaut" %>">
    <meta name="twitter:url" content="<%= @current_page_url %>">
    <meta name="twitter:image" content="/assets/images/favicon.svg">
  </head>
  <body>
    <header class="d-flex align-items-center justify-content-center">
      <nav class="navbar navbar-expand-lg header-nav">
        <a class="navbar-brand" href="/">
          <p class="pen green" style="margin-bottom:0">Cuisine Kitch</p>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
          <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
            <li class="nav-item <%= 'active' if current_page?(recipes_path) %>">
              <%= link_to "Accueil", recipes_path, class: "nav-link" %>
            </li>
            <li class="nav-item <%= 'active' if current_page?(recipes_apropos_path) %>">
              <%= link_to "À propos", recipes_apropos_path, class: "nav-link" %>
            </li>
            <li class="nav-item <%= 'active' if current_page?(new_recipe_path) %>">
              <%= link_to "Nouvelle recette", new_recipe_path, class: "nav-link" %>
            </li>
            <li class="nav-item <%= 'active' if current_page?(categories_path) %>">
              <%= link_to "Catégories", categories_path, class: "nav-link" %>
            </li>
            <li class="nav-item <%= 'active' if current_page?(ingredients_path) %>">
              <%= link_to "Ingrédients", ingredients_path, class: "nav-link" %>
            </li>
          </ul>
        </div>
      </nav>
    </header>
    <%= yield %>
    <%= render 'layouts/footer' %>
  </body>
</html>
